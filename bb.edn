{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([dji_onboard_sdk.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'dji_onboard_sdk.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
